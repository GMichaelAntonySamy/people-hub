import React from "react";
import Headings from "../shared/heading";

const UpcommingEvents = () => {
  return (
    <div>
        <Headings text={'Upcomming Events'}/>
    </div>
  );
};

export default UpcommingEvents;

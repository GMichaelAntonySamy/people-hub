import React from "react";
import Headings from "../shared/heading";

const NewAndEvents = () => {
  return (
    <div>
        <Headings text={'New & Events'}/>
    </div>
  );
};

export default NewAndEvents;
